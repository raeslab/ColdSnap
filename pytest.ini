[pytest]
pythonpath = src
# Note: Configuration also available in pyproject.toml [tool.pytest.ini_options]
# Only suppress specific known warnings to avoid hiding legitimate issues
filterwarnings =
    # Add specific warnings to ignore here when needed
    # Example: ignore::DeprecationWarning:shap.*